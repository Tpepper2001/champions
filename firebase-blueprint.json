{
  "entities": {
    "siteContent": {
      "title": "SiteContent",
      "description": "Global website content configuration containing branding, sections, and media",
      "type": "object",
      "properties": {
        "contentJson": {
          "type": "string",
          "description": "Stringified JSON representation of the complete website data"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of the latest content update"
        }
      },
      "required": ["contentJson", "updatedAt"]
    }
  },
  "firestore": {
    "/site_settings/{settingId}": {
      "schema": "siteContent",
      "description": "Site level settings including active published content"
    }
  }
}
